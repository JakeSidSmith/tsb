// eslint-disable-next-line @typescript-eslint/no-inferrable-types
export const message: string = 'I am aliased!';

export const enum ConstEnum {
  FOO = 'FOO',
  BAR = 'BAR',
}

export enum Enum {
  FOO = 'FOO',
  BAR = 'BAR',
}
